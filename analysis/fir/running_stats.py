import os
import re
import numpy as np
import nibabel as nib


# Run stat tests based on nifti files of correlation coefficients


# hemisphere t-test

# need to match up by model - can do best models to reduce false positive rate

# group statistics? 

# will be by layer

