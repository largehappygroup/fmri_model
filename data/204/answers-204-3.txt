participant-id,category,timestamp,prompt,answer,stimulus-id,previous-delay,stimulus_time
participant-id,category,timestamp,prompt,answer,stimulus-id,previous-delay,stimulus_time
204,3,2418228759.998646,Implement a function countdown() that accepts an integer n as a parameter and counts down from n, printing each number to standard output, separated by a space. After reaching n, print "liftoff!",void downcount(int n) {
  for(int i = 0; i < 1; i++) {
    cout << n << " ";
    n--;
  }
  
},8,14476.37782336758,60000
204,3,2418293587.151886,Write a statement that toggles the value of the bool variable onOffSwitch. That is, if onOffSwitch is false, itsvalue is changed to true; if onOffSwitch is true, its value is changed to false.,if(onOffSwitch)
  onOffSwitch = false;
else
  onOffSwitch = true;,1,4790.474320772533,60000
participant-id,category,timestamp,prompt,answer,stimulus-id,previous-delay,stimulus_time
204,3,2418359806.067072,Clunker Motors Inc. is recalling all vehicles in its Extravagant line from model years 1999-2002. Given an int variable modelYear and a string modelName, print "RECALL" if modelYear and modelName match the recal details,void recall(int modelYear, string modelName) {
  if(modelYear >=1999 && modelyear <=2002)
    cout << "RECALL" << endl;
},0,6190.533629585806,60000
204,3,2418425952.87266,Write the definition of a function isPositive, that receives an integer parameter and returns true if the parameter is positive, and false otherwise.,bool isPositive(int x) {
  if(x >= 0)
    return true;
  return false;
},2,6093.986144161348,60000
participant-id,category,timestamp,prompt,answer,stimulus-id,previous-delay,stimulus_time
204,3,2418492100.398986,Given a string, s, and an integer, x, update s such that its characters are shifted by x. Ex. s = "cd", x = 1. Loop results in s = "de",string shiftString(int x, string s) {
  string temp;
  for(int i = x; i < s.size(); i++)
},4,6117.637017661351,60000
204,3,2418558253.641836,Implement a function containsDuplicate that accepts a vector of integers and returns true if it contains a duplicate element, and false otherwise,bool containsDuplicate(vector x) {
  return x.unique()
},6,6128.796685499343,60000
participant-id,category,timestamp,prompt,answer,stimulus-id,previous-delay,stimulus_time
204,3,2418622755.954967,Given integer variables amount and total both initialized to 0, write a loop that reads in values for amount from standard in and adds them to total until total is greater than 100.,Don't remember cin format... :(
  
amount = cin.value;
total += amount;,5,4480.09989400931,60000
participant-id,category,timestamp,prompt,answer,stimulus-id,previous-delay,stimulus_time
204,3,2418686091.10679,Implement a function secondGreatest that accepts a vector of integers and returns the second greatest element in the vector,int secondGreatest(vector x) {
  if(x.size() == 1)
    return x[0];
  else if(x.size() == 2)
},7,3309.0409779605857,60000
204,3,2418749536.635856,Implement a function is_sorted that accepts a vector of integer values and returns true if it is non-decreasing, and false otherwise,bool is_sorted(vector x) }
  for(int i = 0; i < x.size(); i++) {
    if(x[])
  }
},3,3413.0535069821417,60000
