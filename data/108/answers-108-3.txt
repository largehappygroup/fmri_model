participant-id,category,timestamp,prompt,answer,stimulus-id,previous-delay,stimulus_time
108,3,352195388.726242,Write the definition of a function isPositive, that receives an integer parameter and returns true if the parameter is positive, and false otherwise.,bool isPositive(int p){
  return p >0;
},2,16802.665306625597,60000
participant-id,category,timestamp,prompt,answer,stimulus-id,previous-delay,stimulus_time
108,3,352258451.85358,Implement a function containsDuplicate that accepts a vector of integers and returns true if it contains a duplicate element, and false otherwise,bool containsDuplicate(vector<int> elems) {
  int s = elems.size();
  for (int i = 0; i < s; )
},6,3034.4481500124475,60000
108,3,352321436.229737,Implement a function countdown() that accepts an integer n as a parameter and counts down from n, printing each number to standard output, separated by a space. After reaching n, print "liftoff!",void countdown()int x {
  for (itn i=-0 ; i < x; i ++) {
    std::cout << x-i << endl;
  }
  
},8,2961.6589086464132,60000
participant-id,category,timestamp,prompt,answer,stimulus-id,previous-delay,stimulus_time
108,3,352387953.264686,Given integer variables amount and total both initialized to 0, write a loop that reads in values for amount from standard in and adds them to total until total is greater than 100.,total = 0;
while (total <= 100) {
  x
  cin >> x;
  total += x;
},5,6499.643301466294,60000
108,3,352451801.660314,Given a string, s, and an integer, x, update s such that its characters are shifted by x. Ex. s = "cd", x = 1. Loop results in s = "de",for (int i=0; i< s.len(); i++) {
  s[i] = ord(s[i]) + 1;
},4,3825.526605585814,60000
participant-id,category,timestamp,prompt,answer,stimulus-id,previous-delay,stimulus_time
108,3,352514959.238687,Implement a function secondGreatest that accepts a vector of integers and returns the second greatest element in the vector,int secondGreatest(vector<int> nuns) {
  sort(nuns);
  return nuns[nuns.size()-2];
},7,3127.3443315372915,60000
108,3,352579589.424014,Write a statement that toggles the value of the bool variable onOffSwitch. That is, if onOffSwitch is false, itsvalue is changed to true; if onOffSwitch is true, its value is changed to false.,onOffSwitch = !onOffSwitch;,1,4592.652693779788,60000
participant-id,category,timestamp,prompt,answer,stimulus-id,previous-delay,stimulus_time
108,3,352646917.110795,Implement a function is_sorted that accepts a vector of integer values and returns true if it is non-decreasing, and false otherwise,bool is_sorted(vector<int> nums) {
  for(int i=-0; i < nums.size(); i++) {
    if nums[i
  }
},3,7308.869068441795,60000
participant-id,category,timestamp,prompt,answer,stimulus-id,previous-delay,stimulus_time
108,3,352713794.862924,Clunker Motors Inc. is recalling all vehicles in its Extravagant line from model years 1999-2002. Given an int variable modelYear and a string modelName, print "RECALL" if modelYear and modelName match the recal details,if(modelYear >1998) {
  if (modelUYear < 2003) P
},0,6847.191633904558,60000
